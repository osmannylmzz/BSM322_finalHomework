<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="finalHomework.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pages="clr-namespace:finalHomework.Pages">

    <FlyoutItem Title="🔐 Giriş" Route="home">
        <ShellContent ContentTemplate="{DataTemplate pages:HomePage}" />
    </FlyoutItem>

    <FlyoutItem Title="💱 Kurlar" Route="currency">
        <ShellContent ContentTemplate="{DataTemplate pages:CurrencyPage}" />
    </FlyoutItem>

    <FlyoutItem Title="📰 Haberler" Route="news">
        <ShellContent ContentTemplate="{DataTemplate pages:NewsPage}" />
    </FlyoutItem>

    <FlyoutItem Title="🌦️ Hava Durumu" Route="weather">
        <ShellContent ContentTemplate="{DataTemplate pages:WeatherPage}" />
    </FlyoutItem>

    <FlyoutItem Title="✅ Yapılacaklar" Route="todo">
        <ShellContent ContentTemplate="{DataTemplate pages:ToDoPage}" />
    </FlyoutItem>

    <FlyoutItem Title="⚙️ Ayarlar" Route="settings">
        <ShellContent ContentTemplate="{DataTemplate pages:SettingsPage}" />
    </FlyoutItem>

</Shell>
